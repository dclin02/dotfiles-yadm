return {
  {
    "HiPhish/rainbow-delimiters.nvim",
    event = "BufReadPost", -- Lazy load the plugin
    config = function()
      -- Optional: You can add specific configuration here if needed.
      -- No configuration is needed to get started as it has reasonable defaults.
      -- For advanced options, refer to the plugin's documentation.
    end,
  },
}
