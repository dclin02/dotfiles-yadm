#!/usr/bin/env bash
# Wayland version of blur-lock: uses grim + swaylock instead of scrot + i3lock

PICTURE=/tmp/swaylock.png

grim $PICTURE
convert $PICTURE -blur 5x4 $PICTURE
swaylock -i $PICTURE
rm -f $PICTURE
